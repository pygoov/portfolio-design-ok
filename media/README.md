# Media Layer

This directory is the planned canonical home for curated portfolio media.

Intended split:
- `media/source/` for selected masters
- `media/web/` for optimized site-ready assets

Important rules:
- keep raw intake material in `tmp/Портфолио/` until it is curated
- publish from `media/web/`, not from raw source folders
